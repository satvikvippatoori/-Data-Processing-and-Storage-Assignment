# In-Memory Transactional Key-Value Store

## 🛠 How to Run

1. Make sure you have Python 3 installed.
2. Clone or download this repository.
3. Run the test script using:

```bash
python test_db.py


To make this assignment more official, the instructions could clarify whether get() inside a transaction should return uncommitted changes or only committed values. Adding required support for delete(key) and nested transactions would increase complexity and realism. The assignment could also include a few predefined test cases for automatic grading, ensuring consistency across submissions. Finally, providing a sample expected output format would help students verify correctness more easily and reduce ambiguity.